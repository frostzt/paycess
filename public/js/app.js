/* eslint-disable */
// Imports
import callMenu from './menu';
import './cursor';

// Definitions
const menuLink = document.getElementById('menu-link');

// Event listeners
menuLink.addEventListener('click', callMenu);
